viewModel.on("customInit", function (data) {
  // 金建收证合同详情--页面初始化
});
viewModel.get("TalentName_TalentName") &&
  viewModel.get("TalentName_TalentName").on("afterValueChange", function (data) {
    // 人才姓名--值改变后
    //获取流水号
  });